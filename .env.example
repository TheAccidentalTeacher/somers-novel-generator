# Environment Variables for Somers Novel Generator

# Frontend Environment Variables (Netlify)
# These need to be set in your Netlify dashboard under Site Settings > Environment Variables

# API Base URL - points to your Railway backend
VITE_API_BASE_URL=https://somers-novel-generator-production.up.railway.app/api

# Note: All VITE_ prefixed variables are available in the frontend build
# Make sure to set VITE_API_BASE_URL in Netlify to your actual Railway URL
